name: ai-studio-env
channels:
  - defaults
dependencies:
  - pip
  - pip:
    - exc-help @ git+ssh://git@github.com/erguzel/exc-help.git # requires private key
    - pytest
    - pytest-html
    - joblib
    - nltk
    - scikit-learn
    - numpy
prefix: ./.convenv
