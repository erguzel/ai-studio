matplotlib==3.11.2
# 2.5.x requires python 3.12; this is the newest release that fits the
# supported floor of 3.11
numpy==2.5.3
scikit-learn==1.9.1
snowballstemmer==3.1.1
# ai-studio itself, from the repo root, installed in place: the version never
# changes, so a plain install would be skipped as satisfied and leave a stale
# copy behind. Install from this directory - pip resolves a relative path
# against the working directory, not against this file.
-e ../..
