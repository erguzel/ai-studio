name: bbc-news-classification-env
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    # pins live in requirements.txt; create this env from this directory
    - -r requirements.txt
prefix: ./.convenv
